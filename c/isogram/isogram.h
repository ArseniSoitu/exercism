#ifndef ISOGRAM_H
#define ISOGRAM_H

#include <stdbool.h>

/*
 * @brief determine if string is isogram
 * @param phrase alphabetic string
 * @retval true if string is isogram, false if not
 */
bool is_isogram(const char phrase[]);

#endif
